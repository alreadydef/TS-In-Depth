/// <reference path="utility-functions.ts" />

const result1 = Utility.maxBooksAllowed(2);
console.log(result1);

import util = Utility.Fees;
const result2 = util.calculateFee(10);
console.log(result2);